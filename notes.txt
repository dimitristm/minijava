todo: change main to accept multiple files at once
according to min 28: easiest solution is to visit each node two times once to get declerations and once to check all references to identifiers
there are no classes defined within classes

train of thought that led to multiple data structures (beyond just SymbolTable):
    produce some information to make sure the same class cannot be defined twice (put them in a new symbolTable or make CLASS a MinijavaType)
    Main class can be handled by exception (but not using an exception could be nicer)}
    multiple different symbol tables for each kind of thing? like one for functions, one for classes one for variables
    it seems that that's needed because a method and a variable can have the same name without conflict in java, while i don't think anything is said about that in minijava
train of thought that led to List<String> for return type
    either return List<String> or give up on calling super.accept because visit functions have side-effects so calling them twice would alter the symbol table
    etc in unpredictable ways.- i decided on list not because it allows me to not write all the visit functions instead of using super.accept, but because it
    lets me concentrate the change of the structures in a few spots instead of having every function be imperative.
    -after writing a huge list of functions that both didn't need to use imperative nor multiple strings, starting to doubt this one


Structures: 
-SymbolTable with tables of type Map<ClassAndIdentifier, varInfo> for fields and local variables
-SymbolTable with tables of type Map<ClassAndIdentifier, methodInfo> for methods (they can have the same name as a field) (i think, find out if that's wrong
and if so, just use the previous symboltable for methods too)
-HashTable<String> for Class names (to check that they've not been declared twice)
-something for class inheritance
---merge the previous 2 structures into one hashmap

f0 -> (x)* means that f0 is a NodeListOptional with elements of type x which you can iterate over with elementAt() and size()


do i have to make sure that "args" is not used since minijava says they should not be used?
"A MiniJava program is a file that begins with a special class that contains the main method and specific ***arguments that are not used***."
this will probably be done automatically because i won't put them in the symbol table
but what if somebody creates a new variable with the name args? (probably nothing because of shadowing)

MethodInfo must have the argumet types in correct order and so must ExpressionList

todo: even better error messages, change the type of the exception that i throw
todo: find some way to make the symboltable print the value (maybe just undo the generics because i won't need them anyway, but hopefully not because i like them)
todo: add the @Override
todo: make stuff more generic by changing LinkedList to List etc wherever possible
todo?: make the formalParameterVisitor add the parameters to the symbol table instead of giving the info to the calle?
todo: Main can be called by other functions and you can make multiple instances of the main class in java. how about minijava and is mine correct?
    old: does main method go into the method HashTable? is there a point? i presume other methods cannot be called main so it doesn't matter
how to get current line number in this tool? it will be usefull for exception messages
todo: bothAreCompatibleType not declared yet error -> does it really get triggered correctly? probs not but i'm not touching it cause it's a risk

todo: the offsets

credit https://stackoverflow.com/questions/4036191/sources-from-subdirectories-in-makefile
if i keep the makefile test thing at the end (probably shouldn't keep because they must have their own tools anyway)

print statement seems to need nothing in and of itself. in java we can print any object/primitive
seems like assignment statements return the left value in java but that has no meaning in minijava 
find every time we use the type of a variable found via Identifier visit function and remove null checks because it can't return null, it throws an exception

all this stuff is checked in the collector visitor:
populating classesAndTheirParents:
    check whether a class has already been defined with that name. if yes, error
    put the name of the class in classesAndTheirParents with null if it's not inheriting
    -but if it's inheriting, make sure the class it's inheriting from has already been defined (cannot define it later, that's invalid)

populating methods:
    if the name of the method has already been defined with another signature in this class or its parent class, error (we have no function overloading)
    make sure it does not have a void return type, if it does, error
    add the needed information to methods

inheritied fields handled by not exiting when class ends, only enter and exit on methods
make more visitors to tidy up code and make other visitors inherit/ use it (like a simple visitor that returns types of primitives or declarations)
tidy up more: make first visitor return just the String that represents the type, if Identifier returns null type it doesn't exist so exception, if we are declaring the identifier then call the StringRepVisitor instead

can void be an argument type to a method?

--------

void methods are apparently handled by grammar so maybe remove the check for that

the identifier visitor that the Type visitor must call should return the string representation if it's a class, or the type if it's an identifier
must distinguish between Type and Identifier
solution:
change identifier to only give the string representation and wherever i  .get(0) use a function instead
but PrimaryExpression needs Identifier to return the type..

return types (except null and list of one element)
VarDeclaration -> type, id (can just insert to symboltable in the VarDeclaration visitor instead of having the callee do it, return null)
Identifier -> type, id but the solution requires this to be just the string representation anyway
ExpressionList returns list of the types of all expressions, make new visitor -> issue: but it relies on the logic of the current TypeCheckVisitor for each simple expression
 -solution: new visitor just has the calle visitor as a field and uses it - done

 TypecheckVisitor will only return strings


 Identifier: 
    pass false - get(0) -> type, get the idName and use getTypeOfVariable - pass false
    pass true  - get(1) -> just remove, idName - pass true

for VarDeclaration i assume that accepting a ( VarDeclaration() )* node will visit all of the children in order, if funky things are happening with the symboltable check this
maybe TypecheckVisitor should inherit from StringRepVisitor

identifier returns type and wherever we don't want the type, call string rep?

use false:
adding to the symboltable -> just return the toString
Type -> identifier -> class name, just return the toString
use true:
PrimaryExpression -> identifier -> parsed name of variable, convert to type
doing static type checking -> identifier -> parsed name of variable, convert to type

cannot directly call a different function in PrimaryExpression because we don't know the type of the child,
not supposed to use reflection

-communicate to the identifier function that we're interested in the type of what's being parsed via argument? 
check for uses of TypecheckVisitor, correct the argument

TypeCheckVisitor takes boolean argument, true = parse identifier as a variable and return the type

could also use a field as the expectingTypeOfIdentifier var, but that's state which is more complex to get right, looks less hacky though (?)
expectingTypeOfIdentifier is not really the best name because we may also get type of a literal in PrimaryExpression visitor func

getTypeOfVariable already checks whether the var exists as an exception, handle the exception where the null checks were for better error messages

TEST 35
---Checking ../minijava-examples-new/minijava-error-extra/test35.java.
SUCCESS: Program ../minijava-examples-new/minijava-error-extra/test35.java passed the semantic check.
what am i supposed to do? there is no real problem with printing that in java, and nothing is said about it in the exericise

double check != and == vs .equals in strings

override func -> no new offset

do something about inheriting/making instances of main class

can you override the main function of a class?